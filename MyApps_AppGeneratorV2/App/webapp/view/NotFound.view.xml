<mvc:View 
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    controllerName="myApps.{{ProjectName}}.controller.NotFound">

	<MessagePage 
        title="{i18n>notFound.title}"
        text="{i18n>notFound.text}"
        description="{i18n>notFound.description}" 
        showNavButton="true"
		navButtonPress="onNavBack"/>
</mvc:View>